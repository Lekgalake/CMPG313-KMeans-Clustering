# K-Means Clustering of Dense Network Topology
This project performs a data-driven analysis of a dense, 600-node network to identify clusters and evaluate energy distributions. Using Python (Pandas, NumPy, and Scikit-Learn), the script groups nodes into clusters based on spatial coordinates and visualizes the results through 2D network graphs and 3D smooth energy surfaces
